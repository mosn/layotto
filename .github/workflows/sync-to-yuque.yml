name: Sync with Yuque
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  sync:
    runs-on:  ubuntu-latest
    steps:
      - uses: 15669072513/gitdoc-sync-docker@main
        with:
          osc_user: ${{ secrets.OSC_USER }} 
          osc_pwd: ${{ secrets.OSC_PWD }}  
          yuqueNamespace: eg6z1a/fofdsy
          yuqueSite: https://mosn-layotto.yuque.com/
          yuqueToken: GMxMZw92ioczAGOCTueGOHSJOo6pr6tGTP3TgXLf
          gitRepo: https://github.com/${{ github.repository }}.git
          gitBranch: main
          gitDocToc: /docs/SUMMARY.md
          gitToken: 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
          gitUser: 15669072513

